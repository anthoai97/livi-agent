interface:
  display_name: "Orchestrate Implementation"
  short_description: "Run implementation agents in Herdr with clear ownership"
  default_prompt: "Use $orchestrate-implementation to create a task worktree, run implementation in Herdr, simplify when requested or warranted, and verify the result before any push or PR publication."
