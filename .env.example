# Server-only credentials. Never prefix secrets with VITE_.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.5-flash-lite
HOST=127.0.0.1
PORT=3001
# Optional absolute directory; defaults to this workspace's .data
# LIVI_DATA_DIR=/absolute/path/to/livi-data
